<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        
        <div>
        {% for article in articles %}
        <article style = " border :2px black solid ; margin:70px; padding :30px; ">
        <h2>Titre de l'article {{article.title}} </h2>
        <div>Ecrit dans le {{article.createdAt | date('d/m/y')}} a {{ article.createdAt | date('H:i') }}</div>
        <div>
        <img src="https://img.freepik.com/photos-gratuite/couple-plein-coup-regardant-lune_23-2150385850.jpg?size=626&ext=jpg&ga=GA1.1.749179356.1703366091" >
        <p>   {{article.content}}</p>
        <a href="{{path(('blog_show'),{ 'id': article.id }) }}" >Lire la suite ...</a>

        </div>
        </article>
        {% endfor %}


        </div>
        
        {% endblock %}
    </body>
</html>
